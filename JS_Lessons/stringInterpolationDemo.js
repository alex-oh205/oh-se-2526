// Operators
const a = 4;
const b = 9;
let result = `The result is: ${a > b ? "True" : "False"}`;

console.log(result);

// Functions
function sum(x, y) {
    return x + y;
}

let total = `The sum of is: ${sum(7, 5)}.`;
console.log(total);